# Noro Manga — دليل الاستخدام

موقع ثابت (بدون سيرفر/بلوجر) بواجهة عربية RTL، مبني على بنية مجلدات بسيطة.
لأن المتصفح لا يستطيع "قراءة محتويات مجلد" تلقائيًا، كل مستوى له ملف JSON صغير يوصف محتواه.

## البنية

```
index.html                              الصفحة الرئيسية (تعرض كل الأعمال)
assets/css/style.css                    التصميم العام
mangamanhwa/
  works.json                            قائمة كل الأعمال (تظهر في الرئيسية)
  One Piece/
    One Piece.html                      صفحة العمل (غلاف + قصة + نوع + حالة)
    cover.svg                           غلاف العمل
    chapters.json                       قائمة فصول هذا العمل
    chapters/
      Chapter.html                      قالب واحد يعرض أي فصل (يقرأ ?work=&ch=)
      1/
        images.json                     ترتيب صور الفصل 1
        1.jpg, 2.jpg, 3.jpg ...
      2/
        images.json
        ...
```

## كيف تضيف عملًا جديدًا (مثلاً "Solo Leveling")

1. انسخ مجلد `mangamanhwa/One Piece` كاملاً وأعد تسميته إلى `Solo Leveling`.
2. أعد تسمية `One Piece.html` داخل المجلد إلى `Solo Leveling.html`.
3. افتح `Solo Leveling.html` وعدّل يدويًا:
   - `<title>`
   - قسم "بيانات العمل" (الغلاف، الاسم، القصة، النوع، الحالة — مانجا/مانهوا، مستمر/متوقف)
   - المتغير `WORK_FOLDER` في أسفل الملف اجعله `"Solo Leveling"`
4. احذف صور الفصول التجريبية داخل `chapters/1` و `chapters/2` وضع صور عملك الحقيقية،
   وعدّل `images.json` في كل فصل ليطابق أسماء الصور الجديدة بالترتيب الصحيح.
5. عدّل `chapters.json` ليطابق عدد الفصول الحقيقي وعناوينها.
6. لا تلمس `Chapter.html` — هو قالب مشترك يعمل تلقائيًا لأي فصل من خلال JSON.
7. أضف العمل الجديد إلى `mangamanhwa/works.json` حتى يظهر في الصفحة الرئيسية:

```json
{
  "id": "solo-leveling",
  "folder": "Solo Leveling",
  "title": "سولو ليفلينغ",
  "cover": "mangamanhwa/Solo Leveling/cover.svg",
  "type": "مانهوا",
  "status": "ongoing",
  "chapters_count": 1
}
```

## كيف تضيف فصلًا جديدًا لعمل موجود

1. أنشئ مجلدًا جديدًا برقم الفصل داخل `chapters/`، مثلاً `chapters/3`.
2. ضع صور الصفحات بالداخل (بأي اسم، المهم الترتيب).
3. أنشئ `images.json` بداخله يسرد أسماء الصور بالترتيب:
   ```json
   { "images": ["1.jpg", "2.jpg", "3.jpg"] }
   ```
4. أضف الفصل إلى `chapters.json` الخاص بالعمل:
   ```json
   { "number": 3, "folder": "3", "title": "الفصل 3", "pages": 3 }
   ```
تظهر الفصول تلقائيًا في صفحة العمل وفي القارئ (أحدث وأقدم، والتنقل بينهما) دون لمس أي كود.

## ملاحظات مهمة

- صور الأغلفة وصفحات الفصل الحالية هي **عناصر نائبة (placeholder) بصيغة SVG** — استبدلها بصورك الحقيقية.
- الموقع بحاجة لأن يُفتح عبر سيرفر محلي أو استضافة فعلية (وليس فتح الملف مباشرة بـ file://)
  لأن المتصفح يمنع طلبات fetch لملفات JSON محليًا لأسباب أمنية. للاختبار محليًا يمكنك تشغيل:
  ```
  python3 -m http.server
  ```
  ثم فتح `http://localhost:8000`
- الخط: Cairo للعناوين، Tajawal للنصوص — يتم تحميلهما من Google Fonts.
- الختم الملون أعلى كل غلاف (أحمر = مستمر، رمادي = متوقف) هو العنصر البصري المميز للموقع.
